<div class="show-item">
  <%= render @user %>
</div>

<nav class="page-nav">
  <% if @user == current_user %>
    <%= link_to t('views.common.edit', name: User.model_name.human.downcase), edit_user_registration_path(@user) %> |
  <% end %>
  <%= link_to t('views.common.back', name: i18n_pluralize(User.model_name.human.downcase)), users_path %>

</nav>
