<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('common.edit'), edit_book_path(@book) %> |
  <%= link_to t('common.index'), books_path %>

  <%= button_to t('common.destroy'), @book, method: :delete %>
</nav>
